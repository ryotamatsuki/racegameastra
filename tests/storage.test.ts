import test from 'node:test';import assert from 'node:assert/strict';import {load,persist,defaults} from '../src/storage/save';import {standard} from '../src/data/catalog';
test('storage handles quota/access denial without throwing',()=>{Object.defineProperty(globalThis,'localStorage',{value:{getItem(){throw Error('denied');},setItem(){throw Error('quota');}},configurable:true});assert.equal(load().version,1);assert.equal(persist(defaults()),false);});
test('storage validates corruption and unknown part IDs',()=>{for(const v of ['{',JSON.stringify({version:1,slots:[{machine:0,setup:{...standard(0),motor:'unknown'}}]})]){Object.defineProperty(globalThis,'localStorage',{value:{getItem:()=>v},configurable:true});assert.deepEqual(load().slots,[null,null,null]);}});
test('three slots and settings restore exactly',()=>{const d=defaults();d.slots=[0,1,2].map(machine=>({machine,setup:standard(machine)}));let raw='';Object.defineProperty(globalThis,'localStorage',{value:{getItem:()=>raw,setItem:(_k:string,v:string)=>{raw=v;}},configurable:true});assert.equal(persist(d),true);assert.deepEqual(load(),d);});
