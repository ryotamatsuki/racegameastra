import * as T from 'three';
import {derive,machines,parts,type Setup,type Category} from '../../data/catalog';
export type CarModel={root:T.Group;groups:Record<Category,T.Group>;wheels:T.Group[];explode:(amount:number)=>void;highlight:(c:Category|null)=>void;};
export function material(color:T.ColorRepresentation,metalness=0,roughness=.45){return new T.MeshStandardMaterial({color,metalness,roughness});}
export function mesh(g:T.BufferGeometry,m:T.Material,parent:T.Object3D,x=0,y=0,z=0){const o=new T.Mesh(g,m);o.position.set(x,y,z);o.castShadow=true;o.receiveShadow=true;parent.add(o);return o;}
export function buildCar(index:number,setup:Setup,lite=false):CarModel{
 const root=new T.Group(),groups={} as Record<Category,T.Group>,wheels:T.Group[]=[];
 for(const c of Object.keys(setup) as Category[]){groups[c]=new T.Group();groups[c].name=c;root.add(groups[c]);}
 const s=derive(setup),color=machines[index].color,paint=material(color,.42,.28),dark=material(0x20282b,.15,.42),rubber=material(0x171b1c,0,.88),silver=material(0xb6c6ca,.85,.23),gold=material(0xe8b759,.75,.32),glass=material(0x142d3f,.68,.15),white=material(0xeceee6,.2,.36);
 const detail=lite?12:24;
 const box=(parent:T.Object3D,m:T.Material,x:number,y:number,z:number,w:number,h:number,d:number)=>mesh(new T.BoxGeometry(w,h,d),m,parent,x,y,z);
 const cyl=(parent:T.Object3D,m:T.Material,x:number,y:number,z:number,r:number,h:number)=>mesh(new T.CylinderGeometry(r,r,h,detail),m,parent,x,y,z);
 // A loft of elliptical cross sections gives each shell a genuinely distinct, curved silhouette.
 const profiles=[[[.073,.006,.011],[.044,.029,.016],[.009,.025,.033],[-.034,.038,.023],[-.068,.044,.009]],[[.065,.020,.016],[.036,.033,.032],[-.004,.034,.044],[-.042,.030,.037],[-.062,.022,.014]],[[.078,.005,.010],[.040,.017,.019],[0,.022,.036],[-.04,.018,.025],[-.075,.012,.010]],[[.074,.003,.012],[.041,.028,.027],[0,.035,.035],[-.04,.029,.029],[-.076,.008,.012]]][index];
 const bodyVariant=parts.body.findIndex(p=>p.id===setup.body),widthScale=bodyVariant===0?.88:bodyVariant===2?1.10:1;
 const vertices:number[]=[],indices:number[]=[];const rings=lite?12:24;
 for(const [x,w,h]of profiles)for(let j=0;j<rings;j++){const a=j/rings*2*Math.PI;vertices.push(x,.016+Math.sin(a)*h*.55+h*.4,Math.cos(a)*w*widthScale);}
 for(let i=0;i<profiles.length-1;i++)for(let j=0;j<rings;j++){const a=i*rings+j,b=i*rings+(j+1)%rings,c=b+rings,d=a+rings;indices.push(a,b,d,b,c,d);}for(const end of [0,profiles.length-1])for(let j=1;j<rings-1;j++)indices.push(end*rings,end*rings+j,end*rings+j+1);
 const geo=new T.BufferGeometry();geo.setAttribute('position',new T.Float32BufferAttribute(vertices,3));geo.setIndex(indices);geo.computeVertexNormals();mesh(geo,paint,groups.body);
 const canopy=mesh(new T.SphereGeometry(1,detail,12),glass,groups.body,index===1?-.006:.001,.046,0);canopy.scale.set(index===1?.023:.028,index===2?.013:.014,index===2?.014:.020);
 // Contrasting inset racing stripes follow the tapered nose, plus vents and a numbered badge.
 const stripe=new T.Shape();stripe.moveTo(.066,-.003);stripe.lineTo(.066,.003);stripe.lineTo(-.058,.006);stripe.lineTo(-.058,-.006);stripe.closePath();const strip=mesh(new T.ShapeGeometry(stripe),white,groups.body,0,.039,0);strip.rotation.x=-Math.PI/2;
 for(const side of [-1,1]){for(let i=0;i<3;i++){const vent=box(groups.body,dark,-.027-i*.008,.039,side*.021,.004,.002,.016);vent.rotation.y=side*.25;}const sill=box(groups.body,paint,0,.018,side*(index===2?.022:.034),.09,.006,.008);sill.rotation.y=side*.10;}
 const chassisColor=setup.chassis==='chassis-light'?0x333b3c:setup.chassis==='chassis-low'?0x495f62:0x777c83;
 const shape=new T.Shape();shape.moveTo(-.078,-.027);shape.lineTo(-.070,-.039);shape.lineTo(.06,-.033);shape.lineTo(.078,-.019);shape.lineTo(.078,.019);shape.lineTo(.06,.033);shape.lineTo(-.070,.039);shape.lineTo(-.078,.027);shape.closePath();const ch=mesh(new T.ExtrudeGeometry(shape,{depth:.004,bevelEnabled:true,bevelSize:.002,bevelThickness:.0015,bevelSegments:2,steps:1}),material(chassisColor,.35,.5),groups.chassis,0,.005,0);ch.rotation.x=-Math.PI/2;
 for(const x of [-.049,.049]){const axle=cyl(groups.chassis,silver,x,.014,0,.002,.095);axle.rotation.x=Math.PI/2;for(const z of [-.044,.044]){const wh=new T.Group();wh.position.set(x,s.radius,z);groups.tire.add(wh);wheels.push(wh);const tyre=cyl(wh,rubber,0,0,0,s.radius,.014);tyre.rotation.x=Math.PI/2;for(const sign of [-1,1]){const rim=cyl(wh,silver,0,0,sign*.0073,s.radius*.72,.0016);rim.rotation.x=Math.PI/2;const hub=cyl(wh,gold,0,0,sign*.0084,s.radius*.25,.002);hub.rotation.x=Math.PI/2;if(!lite)for(let j=0;j<6;j++){const a=j/6*Math.PI*2;const spoke=box(wh,dark,Math.cos(a)*s.radius*.42,Math.sin(a)*s.radius*.42,sign*.0083,s.radius*.45,.002,.001);spoke.rotation.z=a;}}if(setup.tire==='tire-grip'&&!lite)for(let j=0;j<16;j++){const a=j/16*2*Math.PI;const tread=box(wh,material(0x313537),Math.cos(a)*s.radius,Math.sin(a)*s.radius,0,.0015,.0015,.012);tread.rotation.z=a;}}}
 const rollerVariant=parts.roller.findIndex(p=>p.id===setup.roller);for(const x of [-.083,.083]){box(groups.roller,dark,x,.014,0,.014,.004,.126+rollerVariant*.008);for(const side of [-1,1]){const z=side*(.054+rollerVariant*.007);cyl(groups.roller,silver,x,.023,z,.007+rollerVariant*.0015,.006);cyl(groups.roller,gold,x,.027,z,.002,.006);if(rollerVariant===2)cyl(groups.roller,dark,x,.013,z,.009,.003);}}
 for(const z of [-.015,.015]){const b=cyl(groups.battery,setup.battery==='battery-power'?gold:setup.battery==='battery-light'?white:material(0x72b4a3,.3),-.006,.018,z,.007,.050);b.rotation.z=Math.PI/2;for(const x of [-.032,.020]){const terminal=cyl(groups.battery,silver,x,.018,z,.004,.002);terminal.rotation.z=Math.PI/2;}}
 const motor=cyl(groups.motor,setup.motor==='motor-speed'?material(0xdd594c,.6):setup.motor==='motor-torque'?gold:silver,-.048,.022,0,.009,.024);motor.rotation.x=Math.PI/2;box(groups.motor,dark,-.048,.022,.015,.013,.013,.005);
 const gearVariant=parts.gear.findIndex(p=>p.id===setup.gear);for(let i=0;i<2;i++){const r=.006+i*.001+gearVariant*.001;const gear=cyl(groups.gear,i?gold:material(0xf0ede0),-.04+i*.017,.018,.025,r,.004);gear.rotation.x=Math.PI/2;if(!lite)for(let j=0;j<12;j++){const a=j*Math.PI/6;const tooth=box(groups.gear,white,-.04+i*.017+Math.cos(a)*r,.018+Math.sin(a)*r,.025,.003,.003,.004);tooth.rotation.z=a;}}
 const wingVariant=parts.wing.findIndex(p=>p.id===setup.wing),span=.045+wingVariant*.025;for(const z of [-span*.3,span*.3])box(groups.wing,dark,-.061,.040,z,.006,.030,.004);const wing=box(groups.wing,paint,-.065,.055,0,.021,.003,span);wing.rotation.z=-.12;for(const side of [-1,1])box(groups.wing,paint,-.065,.06,side*span*.5,.025,.014,.003);
 const brakeVariant=parts.brake.findIndex(p=>p.id===setup.brake);box(groups.brake,material([0x93ae8a,0xdaac69,0xd16475][brakeVariant],0,.9),-.08,.004,0,.010+brakeVariant*.004,.003,.040);
 if(!lite)for(const x of [-.066,.066])for(const z of [-.025,.025]){cyl(groups.chassis,silver,x,.011,z,.002,.002);box(groups.chassis,dark,x,.0122,z,.003,.0005,.0005);}
 const offsets:Record<Category,T.Vector3>={body:new T.Vector3(0,.10,0),chassis:new T.Vector3(0,0,0),motor:new T.Vector3(-.045,.04,0),gear:new T.Vector3(.02,.065,.035),tire:new T.Vector3(0,.015,.07),roller:new T.Vector3(0,.035,-.055),battery:new T.Vector3(0,.05,0),wing:new T.Vector3(-.045,.10,0),brake:new T.Vector3(-.025,-.025,0)};
 return {root,groups,wheels,explode(amount){for(const c of Object.keys(groups) as Category[])groups[c].position.copy(offsets[c]).multiplyScalar(amount);},highlight(c){root.traverse(o=>{if(o instanceof T.Mesh){const m=o.material as T.MeshStandardMaterial;if(m.emissive)m.emissive.setHex(0);}});if(c)groups[c].traverse(o=>{if(o instanceof T.Mesh){const m=o.material as T.MeshStandardMaterial;m.emissive?.setHex(0x19302a);}});}};
}
export function disposeObject(root:T.Object3D){const mats=new Set<T.Material>(),geos=new Set<T.BufferGeometry>();root.traverse(o=>{if(o instanceof T.Mesh||o instanceof T.LineSegments){geos.add(o.geometry);for(const m of Array.isArray(o.material)?o.material:[o.material])mats.add(m);}});geos.forEach(g=>g.dispose());mats.forEach(m=>m.dispose());root.removeFromParent();}
